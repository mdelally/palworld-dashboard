# Palworld 0.6+ (PlM/Oodle) save support. Stock cheahjs/palworld-save-tools==0.24.0
# only handles PlZ/zlib and cannot read current dedicated-server saves.
#
# Install ONLY the MRHRTZ fork here — it already depends on
# `pyooz @ git+https://github.com/MRHRTZ/pyooz.git`. Listing pyooz a second
# time (even at the same commit) makes pip ResolutionImpossible.
palworld-save-tools @ git+https://github.com/MRHRTZ/palworld-save-tools.git@c42abc210d54b04b10adb46e0ac2332eb7563ef8
